<%= form_for([@exercice, @type_recette], html: {class: "form-horizontal", role: "form"}) do |f| %>
  <% if @type_recette.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_recette.errors.count, "error") %> prohibited this type_recette from being saved:</h2>

      <ul>
      <% @type_recette.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- Formulaire du chantier -->

  <%= hf_text_field(f, :nom) %>
   <%= hf_number_field(f, :poids) %>
  <%= hf_cancel_submit(f, exercice_type_charges_path(@exercice)) %>
<% end %>
